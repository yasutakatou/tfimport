
terraform {
  required_providers {
    aws = {
      source = "hashicorp/aws"
      version = "TFIMPORTPROVIDER"
    }
  }
  required_version = ">= 1.0"
}

provider "aws" {
  region  = "TFIMPORTREGION"
}
